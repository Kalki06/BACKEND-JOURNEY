# Exercises
# movies = ["Dhurandhar", "Sita Ramam", "Avengers","Harry Potter", "Narnia"]
# print(f"{movies[1]} \n{movies[4]}")

# movies[2] = "Avatar"
# print(movies)
# movies.append("KGF")
# print(movies)

# movies.pop(1)
# print(movies)

# for movie in movies:
#     print(movie)

# DSA
# 1 .Find the largest element in a list.
# What is the input?
# elements for the list
# What is the output?
# largest element
# What is the approach?
# create a variable and initialize it with none or minimum integer value usinf sys library and then run a loop over the list and compare each element with the that variable if element is greater that that variable value then variable - elemnt and outside loop return or print  the variable
# What is the time complexity?
# O(n)

# 2.Count positive, negative, and zero values.
# What is the input?
# elements for the list

# What is the output?
# number of positive element, negative element and zero

# What is the approach?
# intialize the three varable with 0 named pos, neg and zero and then run a loop over list and compare each element if element > 0 -> pos++; elif element < 0 neg++ else zero++ then outside loop print pos, neg and zero

# What is the time complexity?
# Big O(n)

# Find the sum of all elements.
# What is the input?
# elements for the list

# What is the output?
#  sum

# # What is the approach?
# create a variable name total and initalize it with zero and then runa loop over list and inside loop total = total + elemnt
# then outsode loop print total

# #What is the time complexity?
# Big O(n)