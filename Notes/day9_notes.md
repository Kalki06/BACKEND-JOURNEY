# Day 9 Notes

## File Handling

-   Learned file modes: r, w, a, x
-   Used `with open()` for safe file handling.
-   Upgraded calculator to save history in `history.txt`.
-   Used a `symbols` dictionary instead of `match`.

## DSA

-   Smallest element: O(n)
-   Count even numbers: O(n)

## Key Lessons

-   Save history immediately after calculations.
-   Use append mode (`a`) to preserve history.
-   Next topic: Exception Handling (`try`, `except`, `finally`).
